entity_type: Kafka
rules:
  - name: broker_down
    state: UNHEALTHY
    duration: 120
    promql: "kafka_brokers or vector(0)"
    threshold: 1
    operator: "<"
    reason: "Kafka broker count {{ value }} < 1 for >2m"
