entity_type: Database
rules:
  - name: unreachable
    state: UNHEALTHY
    duration: 60
    promql: "pg_up or vector(0)"
    threshold: 1
    operator: "<"
    reason: "Postgres unreachable for >60s"
